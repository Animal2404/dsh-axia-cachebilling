# dsh-axia-cachebilling bundle patch: inserts this plugin into a profile's layer stack.
# Applied when a profile lists this bundle (or wired by dev_install_package).
- insert:
    - id: dsh-axia-cachebilling
      name: 'dsh-axia-cachebilling'
      config:
        enabled: true
